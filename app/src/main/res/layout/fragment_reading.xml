<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >
    
    <data>
        <variable
            name="vm"
            type="com.ihfazh.ksatriamuslim.vm.ReadingViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".fragments.ReadingFragment">


        <ImageView
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:id="@+id/background_image"
            android:contentDescription="@string/background_image" />

        <TextView
            android:layout_marginBottom="25dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{vm.mainText}"
            tools:text="Ini Adalah text yang akan dibaca sama anak anak \n Perlu yang panjang sekali sebetulnya"
            android:textSize="35sp"
            android:textAlignment="center"
            android:fontFamily="@font/playtime"
            android:paddingStart="100dp"
            android:paddingEnd="100dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:id="@+id/main_text"
            android:textColor="@color/black"
            />

        <ImageButton
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:id="@+id/next_icon"
            android:src="@drawable/next_button"
            android:scaleType="centerInside"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginEnd="30dp"
            android:layout_marginBottom="25dp"
            android:background="@null"
            android:clickable="@{vm.hasNext}"
            android:enabled="@{vm.hasNext}"
            android:onClick="@{() -> vm.nextPage()}"
            android:contentDescription="@string/next_page" />

        <ImageButton
            android:enabled="@{vm.hasPrev}"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:id="@+id/prev_icon"
            android:src="@drawable/prev_button"
            android:background="@null"
            android:scaleType="centerInside"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginStart="30dp"
            android:layout_marginBottom="25dp"
            android:clickable="@{vm.hasPrev}"
            android:onClick="@{() -> vm.prevPage()}"
            android:contentDescription="@string/prev_page" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="1"
            android:text='@{String.valueOf(vm.page)}'
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginBottom="25dp"
            android:fontFamily="@font/playtime"
            android:textSize="20sp"
            android:textColor="@color/black"
            android:id="@+id/page_number"
            />


    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
